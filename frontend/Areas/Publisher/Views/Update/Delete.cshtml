@model Localactors.entities.update

@{
    ViewBag.Title = "Delete";
}

<ul class="breadcrumb">
    <li><a href='@Url.Action("Index", "Home", new { Area = "Publisher" })'>Publisher Home</a>
        <span class="divider">/</span></li>
    <li><a href='@Url.Action("Index", "Home", new { Area = "Publisher" })'>Projects</a>
        <span class="divider">/</span></li>
    <li><a href='@Url.Action("Edit", "Projects", new { id = @ViewBag.Project.ProjectID, Area = "Publisher" })'>
            @ViewBag.Project.Title</a> <span class="divider">/</span></li>
    <li><a href='@Url.Action("Index", "Update", new { projectid = @ViewBag.Project.ProjectID, Area = "Publisher" })'>
            Updates</a>  <span class="divider">/</span></li>
    <li class="active"><a href='@Url.Action("Delete", "Update", new {id=Model.UpdateID, projectid = @ViewBag.Project.ProjectID, Area = "Publisher" })'>
                           Delete Update</a> </li>
</ul>

<h3>Are you sure you want to delete this?</h3>
<fieldset>


    <div class="display-label">project</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.project.Title)
    </div>

    <div class="display-label">Title</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Title)
    </div>

    <div class="display-label">Date</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Date)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" class="btn"  value="Delete" /> 
    </p>
}
